/* historical.js - Complete Redesign - Part 1: Core, Sessions, Navigation */
(async function(){'use strict';
const CONVEX_URL=window.CONFIG?.CONVEX_URL||'';
let convexReady=false;
if(CONVEX_URL&&window.ConvexBridge){try{convexReady=await ConvexBridge.init(CONVEX_URL)}catch(e){console.error('Convex init failed',e)}}
const S={sessions:[],activeSessionId:null,activeSessionMeta:null,data:[],compareData:[],charts:{},map:null};
const $=id=>document.getElementById(id);
const }=sel=>document.querySelectorAll(sel);
const fmt=(v,d=1)=>v==null?'—':Number(v).toFixed(d);
const fmtInt=v=>v==null?'—':Number(v).toLocaleString();
const fmtTime=ms=>{if(ms==null||ms<=0)return'—';const m=Math.floor(ms/60000),s=Math.floor((ms%60000)/1000);return m>0?m+'m '+s+'s':s+'s'};
function toast(msg){let el=document.querySelector('.ha-toast');if(!el){el=document.createElement('div');el.className='ha-toast';document.body.appendChild(el)}el.textContent=msg;el.classList.add('show');clearTimeout(el._t);el._t=setTimeout(()=>el.classList.remove('show'),2500)}
/* Stats Engine */
function mean(a){return a.length?a.reduce((s,v)=>s+v,0)/a.length:0}
function median(a){if(!a.length)return 0;const s=[...a].sort((x,y)=>x-y),m=Math.floor(s.length/2);return s.length%2?s[m]:(s[m-1]+s[m])/2}
function stddev(a){const m=mean(a);return Math.sqrt(a.reduce((s,v)=>s+(v-m)**2,0)/(a.length||1))}
function percentile(a,p){const s=[...a].sort((x,y)=>x-y),i=(p/100)*(s.length-1),lo=Math.floor(i),hi=Math.ceil(i);return lo===hi?s[lo]:s[lo]+(s[hi]-s[lo])*(i-lo)}
function skewness(a){const m=mean(a),s=stddev(a);if(s===0)return 0;return a.reduce((sum,v)=>sum+((v-m)/s)**3,0)/a.length}
function kurtosis(a){const m=mean(a),s=stddev(a);if(s===0)return 0;return a.reduce((sum,v)=>sum+((v-m)/s)**4,0)/a.length-3}
function pearson(x,y){const n=Math.min(x.length,y.length);if(n<3)return 0;const mx=mean(x.slice(0,n)),my=mean(y.slice(0,n));let num=0,dx2=0,dy2=0;for(let i=0;i<n;i++){const dx=x[i]-mx,dy=y[i]-my;num+=dx*dy;dx2+=dx*dx;dy2+=dy*dy}const d=Math.sqrt(dx2*dy2);return d?num/d:0}
function linReg(x,y){const n=Math.min(x.length,y.length);if(n<3)return{slope:0,intercept:0,r2:0};const mx=mean(x.slice(0,n)),my=mean(y.slice(0,n));let ssxy=0,ssxx=0,ssyy=0;for(let i=0;i<n;i++){const dx=x[i]-mx,dy=y[i]-my;ssxy+=dx*dy;ssxx+=dx*dx;ssyy+=dy*dy}const slope=ssxx?ssxy/ssxx:0;const intercept=my-slope*mx;const r2=(ssxx&&ssyy)?(ssxy*ssxy)/(ssxx*ssyy):0;return{slope,intercept,r2}}
/* Sessions */
async function loadSessions(){const el=h-sessions-list;el.innerHTML='<div class= ha-loading><div class=ha-spinner></div><span>Loading sessions…</span></div>';try{const res=await ConvexBridge.listSessions();S.sessions=res?.sessions||(Array.isArray(res)?res:[]);renderSessions()}catch(e){console.error(e);el.innerHTML='<div class=ha-empty><div class=ha-empty-icon>⚠️</div>Failed to load sessions</div>'}}
let historicalLimit=Infinity;
function renderSessions(){const q=(h-search?.value||'').toLowerCase();const sort=h-sort?.value||'newest';let list=S.sessions.filter(s=>(s.session_name||s.session_id||'').toLowerCase().includes(q));if(sort==='newest')list.sort((a,b)=>new Date(b.start_time||0)-new Date(a.start_time||0));else if(sort==='oldest')list.sort((a,b)=>new Date(a.start_time||0)-new Date(b.start_time||0));else if(sort==='most-records')list.sort((a,b)=>(b.record_count||0)-(a.record_count||0));else if(sort==='name-asc')list.sort((a,b)=>(a.session_name||'').localeCompare(b.session_name||''));if(isFinite(historicalLimit)&&historicalLimit>0)list=list.slice(0,historicalLimit);const tot=S.sessions.reduce((s,x)=>s+(x.record_count||0),0);h-explorer-stats.innerHTML='<span>'+S.sessions.length+'</span> sessions · <span>'+tot.toLocaleString()+'</span> total records · <span>'+list.length+'</span> shown';if(!list.length){h-sessions-list.innerHTML='<div class=ha-empty><div class=ha-empty-icon>📭</div>No sessions matching filter</div>';return}h-sessions-list.innerHTML=list.map(s=>{const nm=s.session_name||'Unnamed Session',id=s.session_id||'',dt=s.start_time?new Date(s.start_time).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric',hour:'2-digit',minute:'2-digit'}):'',ct=s.record_count||0,dur=s.duration_s?fmtTime(s.duration_s*1000):'';return'<div class=ha-card ha-session-card ha-animate-in data-sid=+id+><div class=ha-scard-top><div class=ha-scard-name>'+esc(nm)+'</div><div class=ha-scard-date>'+dt+'</div></div><div class=ha-scard-meta><span><b>'+fmtInt(ct)+'</b> records</span>'+(dur?'<span>⏱ '+dur+'</span>':'')+'</div><div class=ha-scard-sparkline><canvas data-sid=+id+></canvas></div><div class=ha-scard-bottom><div class=ha-scard-id>'+id.slice(0,10)+'…</div><div class=ha-scard-badge>'+fmtInt(ct)+'</div></div></div>'}).join('');}('.ha-session-card').forEach(c=>c.addEventListener('click',()=>openSession(c.dataset.sid)))}
function esc(s){const d=document.createElement('div');d.textContent=s;return d.innerHTML}
h-search?.addEventListener('input',renderSessions);
h-sort?.addEventListener('change',renderSessions);
/* Normalize */
function normalizeRecord(r){const n={...r};n._ts=new Date(r.timestamp).getTime();n.speed_kmh=r.speed_ms!=null?r.speed_ms*3.6:(r.speed_kmh||r.avg_speed_kmh||0);n.speed_ms=r.speed_ms!=null?r.speed_ms:n.speed_kmh/3.6;n.power_w=r.power_w!=null?r.power_w:((r.voltage_v||0)*(r.current_a||0));n.voltage_v=r.voltage_v||0;n.current_a=r.current_a||0;n.throttle_pct=r.throttle_pct||r.throttle||0;n.brake_pct=r.brake_pct||r.brake||0;n.throttle_intensity=r.throttle_intensity||null;n.brake_intensity=r.brake_intensity||null;n.accel_x=r.accel_x||0;n.accel_y=r.accel_y||0;n.accel_z=r.accel_z||0;n.g_force=r.current_g_force||(Math.sqrt(n.accel_x**2+n.accel_y**2+n.accel_z**2)/9.81);n.max_g_force=r.max_g_force||n.g_force;n.accel_magnitude=r.accel_magnitude||r.total_acceleration||0;n.lat=r.latitude||0;n.lon=r.longitude||0;n.alt=r.altitude_m||0;n.elevation_gain_m=r.elevation_gain_m||0;n.efficiency=r.current_efficiency_km_kwh??null;n.cumEnergy=r.cumulative_energy_kwh??null;n.routeDist=r.route_distance_km??null;n.energy_j=r.energy_j||0;n.distance_m=r.distance_m||0;n.avg_speed_kmh=r.avg_speed_kmh||0;n.max_speed_kmh=r.max_speed_kmh||0;n.avg_power=r.avg_power||0;n.avg_voltage=r.avg_voltage||0;n.avg_current=r.avg_current||0;n.max_power_w=r.max_power_w||0;n.max_current_a=r.max_current_a||0;n.optimalSpeed=r.optimal_speed_kmh??null;n.optimalEfficiency=r.optimal_efficiency_km_kwh??null;n.optimalConfidence=r.optimal_speed_confidence??null;n.motionState=r.motion_state||null;n.driverMode=r.driver_mode||null;n.qualityScore=r.quality_score??null;n.outlierSeverity=r.outlier_severity||null;n.outliers=r.outliers||null;return n}